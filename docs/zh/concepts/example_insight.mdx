---
weight: 20
---

:::tip

对复杂概念进行深入浅出的介绍。

可以访问[深入理解概念文档示例](https://product-doc-guide.alauda.cn/template/concept/insight_example.html)查看对应文档的示例。

:::

# 深入理解 XXX

增加一个段落，简单介绍 \<名称\> 的含义，让用户对于 \<名称\> 有一个初步的认识。

## 名词解释（可选）

使用表格的方式简单介绍本篇文档中出现的术语，只有用户了解了这些术语后才能读懂这篇文档。

## 为什么需要 \<名称\>

这里可以增加以下几个部分：

1. **第一部分**：交代 \<名称\> 出现的背景或解决了什么样的问题，可通过如下手法写作：

   - **使用对比的手法**：传统的场景中，\<名称> 是如何工作的，但是出现了什么样的问题。
   - **使用叙述的手法**：在什么样的背景下，对于新的挑战或问题，顺理成章的出现了 \<名称> 的概念。

2. **第二部分**：简述 \<名称\> 是如何解决上述问题的；或如何实现新的功能，因此我们才需要 \<名称\>。

## 优势（可选）

使用列表的方式，列出 \<名称\> 的优势。

## 适用场景

介绍一个或多个用户可能用到该功能的场景，让用户能够判断是否需要使用该功能。

## 约束与限制（可选）

列出 \<名称\> 的使用约束和限制，可能包含以下内容：

- 操作系统限制
- 网络限制
- 安全限制
- 其他限制（如：需要满足什么条件才能使用该功能）

## 原理

增加原理图和原理的介绍。通过这部分内容，用户可以深入了解 \<名称\> 的功能和实现过程，对于使用平台的功能更加得心应手。

## 配置示例（可选）

增加配置示例，让用户能够更加直观了解 \<名称\> 的功能。

## 与 \<名称\> 相关的重要参数说明（可选）

对于部分功能来说，有些参数非常重要，而且使用一句话无法描述清楚，需要增加一个章节，专门介绍这些参数。

### 参数 1

增加一个段落，通过一句话对参数进行简单描述。

#### 适用场景

#### 约束与限制（可选）

#### 原理/参数说明

#### 配置示例（可选）

...

### 参数 N

## 参考资料（可选）

列出与 \<名称\> 相关的参考资料，让用户能够更好的理解 \<名称\> 的概念。可用的参考资料包括：

- 官方文档链接